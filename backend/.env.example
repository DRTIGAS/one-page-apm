# Example env for backend (rename to .env and fill values)
# Use SendGrid delivery
USE_SENDGRID=true
SENDGRID_API_KEY=SG.xxxxxxxx_your_api_key_here
SENDGRID_FROM=seu-email@seudominio.com
MAIL_TO=destino@example.com

# Optional SMTP fallback (not used when USE_SENDGRID=true)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=seu-email@example.com
SMTP_PASSWORD=sua-senha

# Other settings
SMTP_ASYNC=true
SMTP_TIMEOUT=10
